project(
  'dotconv',
  'cpp',
  version: '0.1.0',
  default_options : ['warning_level=3', 
    'cpp_std=c++23',
    'default_library=static',
    'buildtype=release'])

cc = meson.get_compiler('cpp')

subdir('src')
